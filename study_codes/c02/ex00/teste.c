#include <stdio.h>

int main()
{
	char str1[] = "oi";
	char str2[3];
	int count = 0;
	while (*src != '\0')
	{
		//printf("dest %d: %s\n", count ,dest);
		aux = *src;
		//printf("dest %d: %s\n",count, dest);
		//printf("print loop\n");
		//printf("scr: %s\n", src);
		count++;
		src++;
	}
	dest = aux;
	printf("dest: %s\n", dest);
}