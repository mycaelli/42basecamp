// malloc -> aloca um pedaco consecutivo de memoria -> array // retorna um ponteiro de tipo void (tem que fazer o cast) //nao inicializa a memoria alocada //se nao tiver espaco para a alocacao, retorna um NULL pointer // sintax -> ptr = (typecast*) malloc(bytesize)
// 	bytesize -> sizeof operator
// 		 -> se var eh uma variavel, entao sizeof eh a numero de bytes ocupado por var
// 	sizeof() 


int main()
{
	
	
}
